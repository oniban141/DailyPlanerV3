<Window x:Class="DailyPlaner.Views.Dialogs.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройки" Height="360" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Оформление" Style="{StaticResource PageTitleStyle}" Margin="0,0,0,16"/>

        <CheckBox Grid.Row="1" x:Name="DarkThemeCheckBox" Content="Тёмная тема"
                  FontFamily="Segoe UI" FontSize="14" Checked="DarkThemeCheckBox_Changed"
                  Unchecked="DarkThemeCheckBox_Changed"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Закрыть" Click="Close_Click" Style="{StaticResource BaseButtonStyle}" Width="110"/>
        </StackPanel>
    </Grid>
</Window>
